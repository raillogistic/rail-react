/**
 * Deprecated shim: route definitions moved to `src/routes/links.tsx` so they can be shared
 * by the core UI and app modules.
 */
/**
 * Routes were moved to `src/routes/links.tsx` so they can be shared by core UI and app modules.
 *
 * This file intentionally exports nothing (not backwards-compatible).
 * Update imports to: `@/routes/links`.
 */
export {};
